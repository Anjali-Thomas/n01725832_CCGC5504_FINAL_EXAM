apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "n01725832-chart.fullname" . }}-test-connection"
  labels:
    {{- include "n01725832-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "n01725832-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
